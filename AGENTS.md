# Agent 工程协作约定

## 目标与阅读顺序

交付能在微信小游戏环境维护、验证和发布的轻量游戏。用户已明确开始开发；当前交付 v0.2.0 首轮试玩版。
开始工作时读取本文件、README、`docs/tasks/BOARD.md`、当前任务和相关架构文档。
产品方向已确认Flappy+Roguelike；最新范围以 `docs/design/CHAPTER_EXPANSION.md`、TASK-012 和 ADR-003 为准；旧图鉴保留长期设计，不能把 78 项策划条目当成已实现或已平衡。

## 工作方式

1. 先检查 `git status --short --branch`；保留用户已有修改，禁止重置或覆盖未归属的文件。
2. 为有实质行为变更的工作建立任务卡，写明目标、范围、验收、依赖和负责文件。
3. 一项任务一个短分支，一次提交一个可解释的变更；遵循 `docs/VERSION_CONTROL.md`。
4. 小步实现，优先现有模块；新增依赖、修改平台/渲染方案或持久化格式需记录 ADR。
5. 完成必要检查并记下真实结果；缺少真机、账号或网络时写明未验证项，继续可独立完成的工作。
6. 更新任务状态和必要文档后提交；交付说明包含变更、验证、风险、下一步。

## 代码规则

- `core` 只依赖自身；`game` 只依赖 `core/game`；`scenes` 只依赖 `core/game/scenes`。
- `app` 组装业务；`platform` 只依赖 `core/platform`；`main.ts` 仅组装 `app/platform`。
- 仅 `src/platform/` 允许引用 `wx`、平台类型、平台计时器和全局 API。规则由架构脚本辅助执行。
- 游戏规则必须能脱离微信运行。时钟、随机数和 IO 显式注入；禁止将绘制坐标反向作为玩法状态。
- 使用 TypeScript strict；不使用 `any` 绕过检查，不引入 DOM 假设或未经适配的浏览器包。
- 注册事件必须支持注销；前后台切换不得重复启动循环；恢复后不得累计后台时间。
- 不提交构建产物、密钥、AppSecret、个人微信配置或无来源素材；客户端不得包含服务端秘密。
- `dist/` 为生成目录，不手工编辑。依赖变更同步提交 `package-lock.json`。

## 验收和协作边界

完成前执行 `npm run verify`，相关平台变更按 `docs/TESTING.md` 增加真机验收记录。
纯文档改动可只运行格式检查和链接检查，并说明原因。不要为低影响格式变更添加无意义测试。
默认由一个 agent 按角色依次完成工作；仅当用户明确要求并行协作时再安排多个 agent。
并行时按模块分工、独立分支/worktree，锁文件和公共接口指定单一负责人，主 agent 集成。
任务交接使用 `docs/templates/HANDOFF.md`；独立复核不等于已完成真机验收。

用户已提供GitHub仓库并授权调用微信开发者工具测试和上传：可同步指定远端，并在AppID/候选包可用时执行测试与开发版本上传，不重复索要授权。
不要把上传推断为提交审核或正式公开发布授权。缺AppID时请求项目信息，不请求AppSecret。
已确认CLI即使业务失败也可能退出码为0，必须检查实际错误对象/上传结果。
