# TASK-001 · 本地工程与 agent 协作基线

- 状态：Done
- 负责人：工程 agent
- 里程碑：M0
- 范围：从空目录建立可验证工程、文档、版本与协作约定
- 非目标：具体玩法、公开远端、账号注册、微信上传/发布

## 验收

- [x] 微信小游戏构建入口与纯逻辑/平台适配分层
- [x] agent 约定、任务模板、ADR、项目计划、测试与发布清单
- [x] 锁定依赖、CI 模板、版本与分支流程
- [x] 本地 `npm run verify` 通过
- [x] 发布配置缺失时明确拒绝生成发布包
- [x] Git 初始提交与 `v0.1.0` 基线标签

## 验证记录

2026-09-18（UTC）验证结果：

- 依赖：`npm ci --offline --cache /private/tmp/snappy-bird-npm-cache` 重装成功。
- 本机 Node 26.6.0 完成初轮验证；最终在 Node 24.21.0 上执行 `npm run verify` 全部通过。
- 10 项单元/约束测试 + 1 项产物冒烟通过，格式、源码与测试类型、架构和包检查通过。
- 开发包内部统计 4,486 bytes（不含 map）；发布工具链用显式测试配置验证，统计 2,379 bytes。
- 无 AppID/基础库配置时发布构建失败，并保持原开发产物不变。
- 测试配置不代表真实账号或该基础库兼容性已验证；最终 dist 已恢复为游客开发包。
- 所有本地 Markdown 链接可解析；产物、依赖、个人配置及常见密钥文件已验证被 Git 忽略。
- 已初始化 main，并启用当前仓库的 pre-commit / commit-msg 钩子。

2026-09-18：已按用户提供的信息配置仓库级提交身份 XDwudi，完成初始提交和 `v0.1.0` 基线标签。
远端 origin 已指向用户提供的 GitHub 仓库；读取时远端没有分支，推送状态由 TASK-004 跟踪。
真机、微信开发者工具和远端 CI 不在本任务的已验证范围内，交给 TASK-003/004。
